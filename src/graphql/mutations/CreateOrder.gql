mutation CreateOrder(
  $address: String!
  $payment_id: String!
  $pricing_plans: [ID]!
) {
  createOrder(
    data: {
      address: $address
      payment_id: $payment_id
      pricing_plans: $pricing_plans
    }
  ) {
    data {
      id
    }
  }
}
